# 로컬 개발 전용 설정. `docker compose up` 시 compose.yaml 과 함께 자동으로 병합된다.
# 앱 접속과 DB/Redis 확인을 위해 호스트에 포트를 열어둔다.
# 배포 시에는 -f 로 파일을 명시하므로 이 파일은 적용되지 않는다. (docs/SETUP.md 참고)

services:
  app:
    ports:
      - "8080:8080"

  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"
